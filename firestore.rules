rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 0. Open Access for Development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
